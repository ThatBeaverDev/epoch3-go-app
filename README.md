# epoch3-go-app

This repository contains files for writing an app in go for
[Constellation-epoch3](/ThatBeaverDev/Constellation-epoch3).

To get started, fork this repository and clone it. You can then run
`install.sh`, edit the go code and run `build.sh` to product your app in
`./dist/app.js`. This file can be ran in Constellation.
